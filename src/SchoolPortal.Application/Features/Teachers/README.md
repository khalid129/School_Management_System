# Teachers feature

Structure placeholder for this session. CQRS build-out (Commands/Queries/Handlers/Validators)
follows the Students slice pattern in `../Students/`. Maps to the STAFF table
(`SchoolPortal.Domain.Entities.Staff`).
